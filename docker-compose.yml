services:
  web:
    build: .
    ports:
      - "${HTTP_PORT:-3004}:3004"
      - "${HTTPS_PORT:-3447}:3447"
    environment:
      - NODE_ENV=production
      - API_URL=http://backend:3008
      - USE_HTTPS=true
      - HTTP_PORT=3004
      - HTTPS_PORT=3447
    restart: unless-stopped
