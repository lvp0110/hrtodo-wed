# URL бэкенда — server.js проксирует /api/* сюда, срезая префикс /api.
# В docker-compose дефолт — http://host.docker.internal:3008 (бэк на хосте).
API_URL=http://localhost:3008

# Хост-порт, который пробрасывается в контейнер (на сервере на нём слушает nginx).
HOST_PORT=3005
